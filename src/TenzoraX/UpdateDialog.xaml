<Window x:Class="TenzoraX.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TenzoraX" Height="260" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}" Foreground="{StaticResource TextBrush}"
        WindowStyle="None" AllowsTransparency="True"
        MouseDown="Window_MouseDown">
    <Border CornerRadius="12" Background="{StaticResource BgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="TenzoraX" FontWeight="Bold" FontSize="16" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock x:Name="TxtUpdateMsg" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10" Foreground="{StaticResource TextBrush}"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Installiert:" FontSize="11" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TxtInstalled" Grid.Column="1" FontSize="11" Foreground="{StaticResource TextBrush}"/>
                </Grid>
                <Grid Margin="0,2,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Neu:" FontSize="11" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TxtNewVersion" Grid.Column="1" FontSize="11" Foreground="#10B981" FontWeight="Bold"/>
                </Grid>

                <!-- Download progress section (hidden initially) -->
                <Border x:Name="PanelProgress" Visibility="Collapsed" Margin="0,12,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="TxtProgressStatus" Text="Update wird heruntergeladen..." FontSize="11" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ProgressBar x:Name="ProgressBar" Height="6" Minimum="0" Maximum="100" Value="0"
                                         Foreground="{StaticResource AccentBrush}" Background="#2D2D3A" BorderThickness="0" Margin="0,0,8,0"/>
                            <TextBlock x:Name="TxtProgressPct" Grid.Column="1" Text="0 %" FontSize="11" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="BtnUpdateLater" Content="Später" Click="BtnLater_Click" Width="80" Height="30" FontSize="11" Margin="0,0,8,0"/>
                <Button x:Name="BtnUpdateNow" Content="Jetzt aktualisieren" Click="BtnUpdate_Click" Width="140" Height="30" FontSize="11" FontWeight="Bold" Background="#0F2C3A" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" Foreground="{StaticResource AccentBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
